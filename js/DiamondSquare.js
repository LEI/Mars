function DiamondSquare() {

	DiamondSquare.prototype.generate = function(  ) {
		var mapWidth = 12;
		this.mapSize = mapWidth;
		this.map = [];

		/*if( mapWidth  ) {

		}*/



		/*var space = mapWidth - 1;

		while( space > 1 ) {

			halfSpace = space / 2;

			for( var i = halfSpace; i < mapWidth; i += space ) {
				for( var j = halfSpace; j < mapWidth; j += space ) {

				}
			}

		}*/

		console.log( mapWidth );


	};

	DiamondSquare.prototype.square = function() {
		for( var i = 0; i < mapSize; i++ ) {

		}
	}

}